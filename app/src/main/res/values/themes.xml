<resources>
    <style name="Theme.FileManager" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:textColor">@color/text</item>
        <item name="android:editTextColor">@color/text</item>
        <item name="android:textColorHint">@color/text_transparent</item>
        <item name="android:textSelectHandle">@drawable/select_handle</item>
        <item name="android:splitMotionEvents">false</item>
    </style>

</resources>